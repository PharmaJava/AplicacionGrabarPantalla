# Bytecode / caché de Python
__pycache__/
*.pyc

# Carpetas generadas por PyInstaller (se recrean con build.bat)
/build/
/dist/
/2.0/build/
/2.0/dist/

# Instalador generado por Inno Setup (se recrea con build_installer.bat)
/installer_output/

# Configuración local del editor / Claude Code
.claude/
